import React from 'react';

const Header = () => {
  return (
    <header>
      <h1>Twitter Clone</h1>
      {/* Add more elements for navigation, user information, etc. */}
    </header>
  );
};

export default Header;
